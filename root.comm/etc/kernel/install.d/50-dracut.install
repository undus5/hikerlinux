#!/bin/bash

set -e

[[ $# == 4 ]] || exit 0

cmd="$1"
kver="$2"
dir="$3"
kimg="$4"

[[ "$cmd" == "add" ]] || exit 0
[[ -f $kimg ]] || exit 1

self_dir=$(dirname $(realpath ${BASH_SOURCE[0]}))
${self_dir}/dracut-install.sh "$kver"
